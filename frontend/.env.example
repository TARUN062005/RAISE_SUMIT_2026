# AURA Frontend Environment Configuration
# (Note: All clinical database and LLM serverless credentials must reside strictly in backend/.env)
# Production API origin used by the deployed frontend.
VITE_API_BASE_URL=https://your-backend-domain.example.com
# Local development proxy target for Vite dev server.
VITE_API_PROXY_URL=http://localhost:8000
